// `server-only` is resolved by the Next.js bundler and has no meaning outside
// it. Aliasing it here lets the integration suites import server modules
// directly while the real marker still guards the application build.
export {};
